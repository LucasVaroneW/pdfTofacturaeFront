services:
  - type: web
    name: pdf-to-xml-converter
    env: static
    buildCommand: node set-env.js && npm install && npm run build
    staticPublishPath: ./dist/pdf-to-xml-converter/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
